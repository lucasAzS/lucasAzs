import React from 'react'
import ReactDom from 'react-dom'
import App from './App.js' // Assumes an integration is used to compile MDX -> JS.

const root = ReactDom.createRoot(document.getElementById('root'))
root.render(<App />)